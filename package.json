{
  "name": "budget-app-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c auto \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
